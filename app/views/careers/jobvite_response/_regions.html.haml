.regions
  .row
    -JobviteResponse.get_regions.each do |region|
      .text-center.region.col-sm-3
        =link_to region[:link] do
          %h2=region[:display].html_safe
        %hr
        = JobListing.in_region(region[:slug]).count
        Openings

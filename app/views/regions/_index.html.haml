.regions
  .row
    -@all_regions.all.each do |region|
      .text-center.region.col-sm-3
        =link_to region_path(region.slug) do
          %h2=region.name.upcase.html_safe
          %hr
          = region.job_count
          Openings

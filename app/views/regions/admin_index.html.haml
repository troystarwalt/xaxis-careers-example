%h1 Listing regions

%table
  %thead
    %tr
      %th Name
      %th Slug
      %th
      %th
      %th

  %tbody
    - @all_regions.each do |region|
      %tr
        %td= region.name
        %td= region.slug
        %td= link_to 'Show', region_path(region)
        %td= link_to 'Edit', edit_region_path(region)
        %td= link_to 'Destroy', region_path(region), :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Region', new_region_path
